setup:
  - echo "Running setup commands"
  - echo "Installing dependencies"


instruction: |
  Make sure to run `dbt seed` before running `dbt run`

tools:
  - |
    Name: my-tool
    Description: my-tool is a tool that enables data analysts and engineers to transform data in their warehouse more effectively.
    Example: my-tool run
  - |
    Name: my-tool-2
    Description: my-tool-2 is a tool for building models
    Example: my-tool-2 build

env_vars:
  FIRST_ENV_VAR: first_env_var_value
  SECOND_ENV_VAR: second_env_var_value
